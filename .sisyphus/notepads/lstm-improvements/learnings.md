
## Feature Engineering in `data()` function
- Added new technical indicators (MACD, Bollinger Bands, ATR proxy) and feature engineering (RSI overbought/oversold, MACD bullish cross, Price above EMA200, ROC_10) to the `data()` function in `06_optimizationTimesteps.ipynb`.
- Used `pandas` DataFrame to calculate these features across the entire dataset (`data_strings`) before reshaping it into `(samples, 150, num_features)`.
- Handled NaN values generated by rolling windows using `df.fillna(0, inplace=True)`.
- Added a new `features == 3` option that includes all original and new features (total 22 features).
- The existing `features == 0`, `features == 1`, and `features == 2` logic remains unchanged.

## Task 3: Attention Mechanism
- Added `modelVar == 5` to `fit_lstmModel` in `06_optimizationTimesteps.ipynb`.
- Used Keras Functional API to implement Attention since `Sequential` API doesn't support it easily.
- Architecture: `Input` -> `LSTM(150)` -> `Dropout` -> `LSTM(150)` -> `Dropout` -> `Attention` -> `GlobalAveragePooling1D` -> `Dense(75)` -> `Dropout` -> `Dense(2, sigmoid)`.
- Verified syntax by converting notebook to script and running `py_compile`.
- Added ReduceLROnPlateau callback to fit_lstmModel in 06_optimizationTimesteps.ipynb to dynamically adjust learning rate.
- Changed optimizer to Nadam with a fixed initial learning rate of 0.001 in 06_optimizationTimesteps.ipynb.
- Removed Pandas-based feature engineering in 06_optimizationTimesteps.ipynb and 07_optimizationFeatures.ipynb. The data() function now uses the new columns directly from the CSV.
